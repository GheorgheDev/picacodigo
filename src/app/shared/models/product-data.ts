export interface ProductData {
  game_id?: string,
  name: string;
  distributor?: string,
  stars: number,
  description?: string,
  price: number,
  stock?: number,
  pegi_id?: string, 
  category_id: string,
  mode_id: string,
}